[project]
name = "rust-from-python"
version = "0.1.0"
description = "Learning Rust from a Pythonista's perspective — side-by-side lessons"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
